const messages = [
    "There are 3 phases to the lifecycle of React components; mount, update, and unmount.",
"The mount component is added to the DOM.",
"The update component is for the chamges in the DOM.",
"The unmount component removes it from the DOM."];

console.log(messages);