export function Main(){
    return(
        <main>
            <p>How to use the navbar with the react-router and navlink is pretty simple because navlink replaces the a tag and use to= instead to link and load the pages together.</p>
        </main>
    );

}