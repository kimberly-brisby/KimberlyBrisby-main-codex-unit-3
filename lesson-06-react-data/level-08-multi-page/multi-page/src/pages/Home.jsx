export function Home(){
    
    return(
        <main>
            <p>The way to create a multi-page website in React is by first importing BrowserRouter, Router and route from react after installing them. Then you have to create a pages folder to add all the pages like home and about to that folderin jsx format. In the App.jsx file you and BrowserRouter to the empty tags and align header, routes, route(which will have a different one for each page added like home and about like so path="/" element="/home"to navigate the website)then footer. The nav bar is placed in the header to be able to go from one page to the other.</p>
        </main>
    );
}