export function Header() {
    return 
        <main>
           <p>The way to make sections into a React compoments is by having each part of the HTML into its on file like header, mian, and footer. Once that part is done you can add multiply tags of that same tag.</p> 
        </main>;
    
}