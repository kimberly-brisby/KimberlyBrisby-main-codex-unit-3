export function Main() {
    const message = "Select the trianlge with a bug on it, hit the settings wheel and check the configurations and that they are right. Then you will select the drop down part and select react debugger and hit the play button.";
    return(
        <main>
            <p>{ message }</p>
        </main>
    );
}