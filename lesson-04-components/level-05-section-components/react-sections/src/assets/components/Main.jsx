export function Main () {
    return 
        <Main><p>Sections can be converted into react components by being contained into there one seperate file in a folder.Each file conetains only 1 HTML tag.</p></Main>;
    
}