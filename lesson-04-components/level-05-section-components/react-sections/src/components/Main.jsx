export function Main (){
    return (
        <main><p>In order to convert sections into a react components is to seperate them in a components folder and then add them to there on files.</p></main>
    );
}